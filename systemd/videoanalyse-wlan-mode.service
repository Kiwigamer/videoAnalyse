[Unit]
Description=VideoAnalyse WLAN boot mode manager (safe default + one-shot AP)
After=network-pre.target
Before=network.target
Wants=network-pre.target

[Service]
Type=oneshot
ExecStart=/usr/local/bin/wlan-mode-manager.sh boot
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
